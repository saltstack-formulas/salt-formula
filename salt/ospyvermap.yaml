# -*- coding: utf-8 -*-
# vim: ft=yaml
---
Debian:
  py_ver 'py3'

RedHat:
  py_ver 'py3'

SmartOS:
  py_ver: 'py2'

Suse: {}

Gentoo: {}

Arch: {}

Alpine: {}

FreeBSD:
  py_ver: 'py3'

OpenBSD: {}

# This is *not* used directly with https://repo.saltstack.com
# Rather, this is used with `salt-winrepo-ng`, where the package names are:
# * py_ver salt-minion-py3
Windows:
  py_ver '-py3'

MacOS:
  py_ver 'py3'
